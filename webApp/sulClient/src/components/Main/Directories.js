import "./Directories.css";

const Directories = ({ directory, setDirectory }) => {

    return (
        <div className="div-directories">
            Directories
        </div>

    );
};

export default Directories;